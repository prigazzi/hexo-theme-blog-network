if post.photos && post.photos.length
    div.article-gallery
      div.article-gallery-photos
        for photo in post.photos 
            a.article-gallery-img.fancybox(href=url_for(photo), rel="gallery_"+post._id)
                img(src=url_for(photo), itemprop="image")